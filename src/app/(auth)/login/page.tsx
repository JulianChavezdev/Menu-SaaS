"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";import {useRouter} from "next/navigation";import Link from "next/link";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const {error}=await createClient().auth.signInWithPassword({email,password});if(error)setError(error.message);else router.push("/dashboard")}return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-6"><h1 className="text-2xl font-bold">Accede a tu carta</h1><label className="mt-5 block">Correo<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-1 w-full rounded-lg p-3 text-slate-900"/></label><label className="mt-4 block">Contraseña<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-1 w-full rounded-lg p-3 text-slate-900"/></label>{error&&<p role="alert" className="mt-3 text-red-300">{error}</p>}<button className="mt-5 w-full rounded-lg bg-violet-500 p-3 font-bold">Entrar</button><p className="mt-4 text-sm">¿No tienes cuenta? <Link className="underline" href="/register">Regístrate</Link> · <Link className="underline" href="/forgot-password">Olvidé mi contraseña</Link></p></form></main>}
