"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";import Link from "next/link";
export default function Register(){const [message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const {error}=await createClient().auth.signUp({email:String(f.get("email")),password:String(f.get("password")),options:{emailRedirectTo:`${location.origin}/dashboard`}});setMessage(error?error.message:"Revisa tu correo para confirmar la cuenta.")}return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-6"><h1 className="text-2xl font-bold">Crea tu cuenta</h1><label className="mt-5 block">Correo<input name="email" required type="email" className="mt-1 w-full rounded-lg p-3 text-slate-900"/></label><label className="mt-4 block">Contraseña<input name="password" required minLength={8} type="password" className="mt-1 w-full rounded-lg p-3 text-slate-900"/></label><button className="mt-5 w-full rounded-lg bg-violet-500 p-3 font-bold">Registrarme</button>{message&&<p className="mt-3 text-sm">{message}</p>}<p className="mt-4 text-sm"><Link className="underline" href="/login">Ya tengo cuenta</Link></p></form></main>}
