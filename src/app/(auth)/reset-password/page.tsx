"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";import {useRouter} from "next/navigation";import {PasswordInput} from "@/components/ui/password-input";
export default function Reset(){const[message,setMessage]=useState("");const router=useRouter();return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={async event=>{event.preventDefault();const password=new FormData(event.currentTarget).get("password") as string;const {error}=await createClient().auth.updateUser({password});if(error)setMessage(error.message);else{setMessage("Contraseña actualizada.");router.push("/dashboard")}}} className="glass w-full max-w-md rounded-3xl p-6"><h1 className="text-2xl font-bold">Nueva contraseña</h1><PasswordInput required minLength={8} name="password" placeholder="Mínimo 8 caracteres" className="w-full rounded-lg p-3 text-slate-900"/><button className="mt-4 w-full rounded-lg bg-violet-500 p-3">Actualizar</button>{message&&<p className="mt-3 text-sm">{message}</p>}</form></main>}
