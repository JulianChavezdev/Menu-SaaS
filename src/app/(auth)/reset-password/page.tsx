"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";import {useRouter} from "next/navigation";export default function Reset(){const[m,setM]=useState("");const r=useRouter();return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={async e=>{e.preventDefault();const p=new FormData(e.currentTarget).get("password") as string;const {error}=await createClient().auth.updateUser({password:p});if(error)setM(error.message);else {setM("Contraseña actualizada.");r.push("/dashboard")}}} className="glass w-full max-w-md rounded-3xl p-6"><h1 className="text-2xl font-bold">Nueva contraseña</h1><input required minLength={8} name="password" type="password" placeholder="Mínimo 8 caracteres" className="mt-5 w-full rounded-lg p-3 text-slate-900"/><button className="mt-4 w-full rounded-lg bg-violet-500 p-3">Actualizar</button>{m&&<p className="mt-3 text-sm">{m}</p>}</form></main>}
