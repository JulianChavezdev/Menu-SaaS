import {activeRestaurant} from "@/lib/permissions";import {BackButton} from "@/components/ui/back-button";import {PLAN_LIMITS,planForStatus} from "@/lib/plans";
export default async function Page(){const {supabase,restaurant}=await activeRestaurant();const[{data:subscription},{count:products},{count:categories}]=await Promise.all([supabase.from("subscriptions").select("plan,status,current_period_end").eq("restaurant_id",restaurant.id).maybeSingle(),supabase.from("products").select("id",{count:"exact",head:true}).eq("restaurant_id",restaurant.id),supabase.from("categories").select("id",{count:"exact",head:true}).eq("restaurant_id",restaurant.id)]);const status=subscription?.status??restaurant.subscription_status??"trialing";const plan=planForStatus(status);const limits=PLAN_LIMITS[plan];const productProgress=Math.min(100,Math.round(((products??0)/limits.products)*100));const categoryLimit=Number.isFinite(limits.categories)?limits.categories:null;return <main className="mx-auto max-w-5xl p-4 md:p-6"><div className="border-b border-slate-800 pb-4"><BackButton fallback="/dashboard"/><h1 className="mt-4 text-2xl font-extrabold">Suscripción</h1><p className="mt-1 text-sm text-slate-400">Consulta el plan y sus límites actuales.</p></div><section className="mt-6 max-w-2xl rounded-3xl border border-slate-800 bg-slate-950/40 p-5 shadow-xl md:p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><span className="rounded-full border border-violet-500/30 bg-violet-500/10 px-3 py-1 text-xs font-semibold uppercase text-violet-300">{plan==="trial"?"Prueba":"Plan activo"}</span><h2 className="mt-3 text-2xl font-bold">{plan==="trial"?"Plan de prueba":"Plan Carta"}</h2><p className="mt-1 text-sm text-slate-400">{plan==="trial"?"Hasta 3 productos y un máximo de 5 categorías.":"Hasta 100 productos, vídeos, personalización y QR."}</p></div><span className="rounded-lg border border-amber-500/30 bg-amber-500/10 px-3 py-1 text-xs font-semibold capitalize text-amber-300">{status}</span></div><div className="my-6 border-t border-slate-800"/><Usage label="Productos" used={products??0} limit={limits.products} progress={productProgress}/><div className="mt-5"><Usage label="Categorías" used={categories??0} limit={categoryLimit}/></div>{plan==="trial"&&<p className="mt-6 rounded-xl border border-violet-500/20 bg-violet-500/5 p-4 text-sm text-violet-200">El servidor bloquea automáticamente la creación del cuarto producto o de una sexta categoría.</p>}</section></main>}
function Usage({label,used,limit,progress}:{label:string;used:number;limit:number|null;progress?:number}){const percentage=progress??(limit?Math.min(100,Math.round((used/limit)*100)):0);return <div><div className="flex justify-between text-sm"><span>{label}</span><span>{used} / {limit??"∞"}</span></div>{limit&&<div className="mt-2 h-2.5 overflow-hidden rounded-full bg-slate-900"><div className={`h-full rounded-full ${percentage>=90?"bg-amber-500":"bg-violet-500"}`} style={{width:`${percentage}%`}}/></div>}</div>}
