import {activeRestaurant} from "@/lib/permissions";import {BackButton} from "@/components/ui/back-button";export default async function Page(){const {supabase,restaurant}=await activeRestaurant();const {data}=await supabase.from("subscriptions").select("plan,status,current_period_end").eq("restaurant_id",restaurant.id).maybeSingle();return <main className="mx-auto max-w-5xl p-6"><BackButton fallback="/dashboard"/><h1 className="my-6 text-3xl font-bold">Suscripción</h1><div className="glass mt-6 max-w-lg rounded-2xl p-6"><h2 className="text-xl font-bold">Plan Carta</h2><p className="mt-2 text-slate-300">1 restaurante · hasta 100 productos · vídeos y QR.</p><p className="mt-4">Estado: <b>{data?.status??"trialing"}</b></p><p className="mt-2 text-sm text-slate-400">Stripe se activará al configurar las variables de entorno y webhook.</p></div></main>}
