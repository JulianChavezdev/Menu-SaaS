import {activeRestaurant} from "@/lib/permissions";import {QrCard} from "@/components/dashboard/qr-card";import {BackButton} from "@/components/ui/back-button";export default async function Page(){const {restaurant}=await activeRestaurant();const root=process.env.NEXT_PUBLIC_APP_URL??"http://localhost:3000";return <main className="mx-auto max-w-5xl p-6"><BackButton fallback="/dashboard"/><h1 className="my-6 text-3xl font-bold">Código QR</h1><QrCard url={`${root}/r/${restaurant.slug}`}/></main>}
