import {activeRestaurant} from "@/lib/permissions";import {RestaurantForm} from "@/components/dashboard/panel";import {MediaUpload} from "@/components/dashboard/media-upload";import {BackButton} from "@/components/ui/back-button";export default async function Page(){const {restaurant}=await activeRestaurant();return <main className="mx-auto max-w-5xl p-6"><BackButton fallback="/dashboard"/><h1 className="my-6 text-3xl font-bold">Apariencia</h1><RestaurantForm restaurant={restaurant}/><div className="mt-6 grid max-w-2xl gap-4 md:grid-cols-2"><MediaUpload restaurantId={restaurant.id} kind="logo" label="Logo del restaurante" currentUrl={restaurant.logo_url}/><MediaUpload restaurantId={restaurant.id} kind="cover" label="Portada del restaurante" currentUrl={restaurant.cover_url}/></div></main>}
