import {activeRestaurant} from "@/lib/permissions";import {Categories} from "@/components/dashboard/panel";import {BackButton} from "@/components/ui/back-button";export default async function Page(){const {supabase,restaurant}=await activeRestaurant();const {data}=await supabase.from("categories").select("id,name,slug,sort_order,is_active").eq("restaurant_id",restaurant.id).order("sort_order");return <main className="mx-auto max-w-5xl p-6"><BackButton fallback="/dashboard"/><h1 className="my-6 text-3xl font-bold">Categorías</h1><Categories categories={data??[]}/></main>}
