import Link from "next/link";import {BackButton} from "@/components/ui/back-button";import {activeRestaurant} from "@/lib/permissions";
export default async function Dashboard(){const {restaurant,supabase}=await activeRestaurant();const [{count:products},{count:categories},{count:videos}]=await Promise.all([supabase.from("products").select("id",{count:"exact",head:true}).eq("restaurant_id",restaurant.id),supabase.from("categories").select("id",{count:"exact",head:true}).eq("restaurant_id",restaurant.id),supabase.from("products").select("id",{count:"exact",head:true}).eq("restaurant_id",restaurant.id).not("video_url","is",null)]);const stats=[['Productos',products??0],['Categorías',categories??0],['Vídeos',videos??0],['Estado',restaurant.is_published?'Publicada':'Borrador']];return <main className="mx-auto max-w-5xl p-6"><div className="mb-5"><BackButton fallback="/"/></div><h1 className="text-4xl font-bold">Hola, {restaurant.name}</h1><p className="mt-3 text-slate-300">Gestiona productos, identidad y disponibilidad desde aquí.</p><section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{stats.map(([label,value])=><div key={label} className="glass rounded-2xl p-5"><p className="text-sm text-slate-300">{label}</p><p className="mt-2 text-2xl font-bold">{value}</p></div>)}</section><div className="mt-8 flex flex-wrap gap-3"><Link href="/dashboard/menu" className="rounded-lg bg-violet-500 px-4 py-2 font-semibold">Gestionar carta</Link><a href={`/r/${restaurant.slug}`} target="_blank" className="rounded-lg border border-white/25 px-4 py-2">Ver carta pública</a></div></main>}
