import {activeRestaurant} from "@/lib/permissions";import {RestaurantForm} from "@/components/dashboard/panel";import {BackButton} from "@/components/ui/back-button";export default async function Page(){const {restaurant}=await activeRestaurant();return <main className="mx-auto max-w-5xl p-6"><BackButton fallback="/dashboard"/><h1 className="my-6 text-3xl font-bold">Restaurante</h1><RestaurantForm restaurant={restaurant}/></main>}
