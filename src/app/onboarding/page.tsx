"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {createRestaurant} from "@/app/dashboard/actions";
export default function Onboarding(){const [error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();try{await createRestaurant(new FormData(e.currentTarget));router.push("/dashboard");router.refresh()}catch(e){setError(e instanceof Error?e.message:"No se pudo crear el restaurante")}}return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-6"><h1 className="text-2xl font-bold">Crea tu restaurante</h1><label className="mt-5 block">Nombre<input name="name" required className="mt-1 w-full rounded-lg p-3 text-slate-900"/></label><label className="mt-4 block">Slug público<input name="slug" required placeholder="pizzeria-roma" className="mt-1 w-full rounded-lg p-3 text-slate-900"/></label><label className="mt-4 block">Color principal<input name="primary_color" type="color" defaultValue="#7c3aed" className="mt-1 block h-10"/></label>{error&&<p role="alert" className="mt-3 text-red-300">{error}</p>}<button className="mt-5 w-full rounded-lg bg-violet-500 p-3 font-bold">Crear restaurante</button></form></main>}
