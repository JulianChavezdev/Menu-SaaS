"use client";import {toast} from "sonner";import {updateAppearancePreferences} from "@/app/dashboard/actions";
export function AppearancePreferences({enabled}:{enabled:boolean}){return <form action={async form=>{try{await updateAppearancePreferences(form);toast.success("Preferencias guardadas")}catch(error){toast.error(error instanceof Error?error.message:"No se pudo guardar")}}} className="rounded-2xl border border-slate-800 bg-slate-950/40 p-5"><h2 className="font-bold">Idiomas de la carta</h2><p className="mt-1 text-sm text-slate-400">Permite que el visitante cambie el idioma de los controles públicos entre español e inglés.</p><label className="mt-4 flex items-center gap-3"><input name="language_switcher_enabled" type="checkbox" defaultChecked={enabled} className="h-5 w-5 accent-violet-500"/><span>Mostrar selector de idioma</span></label><button className="mt-4 rounded-lg bg-violet-600 px-4 py-2 font-semibold">Guardar preferencia</button></form>}
