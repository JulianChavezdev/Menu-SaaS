"use client";import Image from "next/image";import {useEffect,useState} from "react";import QRCode from "qrcode";import {toast} from "sonner";
export function QrCard({url}:{url:string}){const[src,setSrc]=useState("");useEffect(()=>{void QRCode.toDataURL(url,{width:512,margin:2,color:{dark:"#0f172a",light:"#ffffff"}}).then(setSrc)},[url]);return <div className="glass max-w-md rounded-2xl p-6 text-center">{src&&<Image className="mx-auto rounded-xl" src={src} alt={`Código QR de ${url}`} width={512} height={512} unoptimized/>}<p className="mt-4 break-all text-sm">{url}</p><div className="mt-4 flex justify-center gap-3"><button onClick={()=>navigator.clipboard.writeText(url).then(()=>toast.success("URL copiada"))}>Copiar URL</button><a href={src} download="carta-qr.png">Descargar PNG</a><a href={url} target="_blank">Abrir carta</a></div></div>}
