"use client";import {useState,useTransition} from "react";import {ArrowDown,ArrowUp,Eye,EyeOff,Pencil,Trash2,X} from "lucide-react";import {toast} from "sonner";import {deleteCategory,reorderCategories,saveCategory,toggleCategory} from "@/app/dashboard/actions";import type {Category} from "@/lib/types";
function reordered(items:Category[],index:number,delta:number){const copy=[...items];const target=index+delta;if(target<0||target>=copy.length)return null;[copy[index],copy[target]]=[copy[target],copy[index]];return copy.map(item=>item.id)}
export function CategoriesManager({categories}:{categories:Category[]}){const[selected,setSelected]=useState<Category|null>(null);const[busy,start]=useTransition();const submit=(form:FormData)=>start(async()=>{try{await saveCategory(form);toast.success(selected?"Categoría actualizada":"Categoría creada");setSelected(null)}catch(error){toast.error(error instanceof Error?error.message:"No se pudo guardar")}});const move=(index:number,delta:number)=>{const ids=reordered(categories,index,delta);if(ids)start(async()=>{await reorderCategories(ids);toast.success("Orden actualizado")})};return <div className="grid gap-6 lg:grid-cols-[360px_1fr]"><form key={selected?.id??"new"} action={submit} className="glass h-fit rounded-2xl p-5"><div className="flex items-center justify-between"><h2 className="font-bold">{selected?"Editar categoría":"Nueva categoría"}</h2>{selected&&<button type="button" aria-label="Cancelar edición" onClick={()=>setSelected(null)}><X/></button>}</div>{selected&&<input type="hidden" name="id" value={selected.id}/>}<label className="mt-4 block">Nombre<input name="name" required defaultValue={selected?.name??""} placeholder="Ej. Entrantes" className="mt-1 w-full rounded-lg p-3 text-slate-900"/></label><button disabled={busy} className="mt-4 rounded-lg bg-violet-500 px-4 py-2 font-semibold disabled:opacity-50">{busy?"Guardando…":selected?"Guardar cambios":"Crear categoría"}</button></form><div className="space-y-2">{categories.map((category,index)=><article key={category.id} className="glass flex flex-wrap items-center justify-between gap-2 rounded-xl p-3"><div><p className="font-semibold">{category.name}</p><p className="text-xs text-slate-400">{category.is_active?"Visible":"Oculta"}</p></div><div className="flex"><button disabled={!index||busy} aria-label="Subir categoría" onClick={()=>move(index,-1)} className="p-2 disabled:opacity-30"><ArrowUp/></button><button disabled={index===categories.length-1||busy} aria-label="Bajar categoría" onClick={()=>move(index,1)} className="p-2 disabled:opacity-30"><ArrowDown/></button><button aria-label={`Editar ${category.name}`} onClick={()=>setSelected(category)} className="p-2"><Pencil/></button><button aria-label="Cambiar visibilidad" onClick={()=>start(async()=>{await toggleCategory(category.id,!category.is_active);toast.success("Visibilidad actualizada")})} className="p-2">{category.is_active?<Eye/>:<EyeOff/>}</button><button aria-label={`Eliminar ${category.name}`} onClick={()=>{if(confirm(`¿Eliminar ${category.name}?`))start(async()=>{try{await deleteCategory(category.id);toast.success("Categoría eliminada")}catch(error){toast.error(error instanceof Error?error.message:"Error")}})}} className="p-2 text-red-300"><Trash2/></button></div></article>)}</div></div>}
