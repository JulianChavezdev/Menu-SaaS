"use client";import {ArrowLeft} from "lucide-react";import {useRouter} from "next/navigation";
export function BackButton({fallback="/"}:{fallback?:string}){const router=useRouter();return <button type="button" onClick={()=>{if(window.history.length>1)router.back();else router.push(fallback)}} className="focus inline-flex items-center gap-2 rounded-lg border border-white/25 px-3 py-2 text-sm font-medium hover:bg-white/10" aria-label="Volver a la página anterior"><ArrowLeft size={17}/>Volver</button>}
