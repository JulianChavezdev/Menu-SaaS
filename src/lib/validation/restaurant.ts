import {z} from "zod";export const restaurantSchema=z.object({name:z.string().min(2).max(80),slug:z.string().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/),primary_color:z.string().regex(/^#[0-9a-fA-F]{6}$/),currency:z.string().length(3),locale:z.string().min(2)});export const productSchema=z.object({name:z.string().min(2).max(100),description:z.string().max(500).optional(),price_cents:z.coerce.number().int().min(0),category_id:z.string().uuid()});
