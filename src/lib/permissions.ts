import {createClient} from "@/lib/supabase/server";import {redirect} from "next/navigation";import {cookies} from "next/headers";import {canCreateCategory,canCreateProduct,planForStatus} from "@/lib/plans";
type Restaurant={id:string;name:string;slug:string;is_published:boolean;subscription_status:"trialing"|"active"|"past_due"|"canceled";language_switcher_enabled?:boolean;primary_color:string;secondary_color:string;description:string|null;phone:string|null;email:string|null;address:string|null;instagram_url:string|null;website_url:string|null;logo_url:string|null;cover_url:string|null;currency:string;locale:string;timezone:string};
export async function activeRestaurant(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const wanted=(await cookies()).get("active_restaurant_id")?.value;let q=s.from("restaurant_members").select("restaurant_id,role,restaurants(*)").eq("user_id",user.id);if(wanted)q=q.eq("restaurant_id",wanted);let {data}=await q.limit(1).maybeSingle();if(!data&&wanted)data=(await s.from("restaurant_members").select("restaurant_id,role,restaurants(*)").eq("user_id",user.id).limit(1).maybeSingle()).data;if(!data)redirect("/onboarding");return {supabase:s,user,member:data,restaurant:data.restaurants as unknown as Restaurant};}
export async function canAddProduct(restaurantId:string){const s=await createClient();const [{count},{data:restaurant}]=await Promise.all([s.from("products").select("id",{count:"exact",head:true}).eq("restaurant_id",restaurantId),s.from("restaurants").select("subscription_status").eq("id",restaurantId).single()]);return canCreateProduct(count??0,planForStatus(restaurant?.subscription_status??"trialing"))}
export async function canAddCategory(restaurantId:string){const s=await createClient();const [{count},{data:restaurant}]=await Promise.all([s.from("categories").select("id",{count:"exact",head:true}).eq("restaurant_id",restaurantId),s.from("restaurants").select("subscription_status").eq("id",restaurantId).single()]);return canCreateCategory(count??0,planForStatus(restaurant?.subscription_status??"trialing"))}
