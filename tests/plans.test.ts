import {describe,expect,it} from "vitest";import {PLAN_LIMITS,canCreateProduct} from "../src/lib/plans";
describe("Carta plan",()=>{it("allows products below the paid limit",()=>expect(canCreateProduct(99)).toBe(true));it("blocks the hundred-and-first product",()=>expect(canCreateProduct(PLAN_LIMITS.carta.products)).toBe(false));it("limits trial to three products",()=>{expect(canCreateProduct(2,"trial")).toBe(true);expect(canCreateProduct(3,"trial")).toBe(false)});it("limits trial to five categories",()=>expect(PLAN_LIMITS.trial.categories).toBe(5));it("defines one restaurant",()=>expect(PLAN_LIMITS.carta.restaurants).toBe(1))})
