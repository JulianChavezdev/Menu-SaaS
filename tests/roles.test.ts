import {describe,expect,it} from "vitest";import {canEditMenu,canManageTeam,canRemoveMember} from "../src/lib/roles";
describe("member permissions",()=>{it("lets owners and admins manage the team",()=>{expect(canManageTeam("owner")).toBe(true);expect(canManageTeam("admin")).toBe(true);expect(canManageTeam("editor")).toBe(false)});it("never allows removing the owner",()=>{expect(canRemoveMember("owner","owner")).toBe(false);expect(canRemoveMember("admin","owner")).toBe(false)});it("lets every initial role edit the menu",()=>{expect(canEditMenu("owner")).toBe(true);expect(canEditMenu("admin")).toBe(true);expect(canEditMenu("editor")).toBe(true)})})
