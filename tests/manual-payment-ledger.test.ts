import {describe,expect,it} from "vitest";
import {filterLedgerPayments,manualPaymentsCsv,monthlyPaymentClosure,safeLedgerDate,safeLedgerMonth,type ManualPaymentRow} from "../src/lib/manual-payment-ledger";
const rows:ManualPaymentRow[]=[{id:"1",amount_cents:2500,currency:"EUR",method:"bizum",paid_at:"2026-07-14T10:00:00Z",period_end:"2026-08-14T10:00:00Z",restaurants:{id:"r1",name:"ILLEGAL FOOD",slug:"illegal-food"}},{id:"2",amount_cents:1000,currency:"USD",method:"bank_transfer",paid_at:"2026-06-10T10:00:00Z",period_end:"2026-07-10T10:00:00Z",restaurants:{id:"demo",name:"Bistro Nube",slug:"bistro-nube"}}];
describe("libro de pagos manuales",()=>{it("combina restaurante, método y fechas excluyendo la demo",()=>{expect(filterLedgerPayments(rows,{q:"illegal",method:"bizum",from:"2026-07-01",to:"2026-07-31"})).toHaveLength(1);expect(filterLedgerPayments(rows,{})).toHaveLength(1)});it("calcula el cierre por mes y moneda",()=>{expect(monthlyPaymentClosure(rows,"2026-07")).toMatchObject({count:1,totals:{EUR:2500},methods:{bizum:1}})});it("valida fechas y meses",()=>{expect(safeLedgerDate("2026-02-30")).toBeNull();expect(safeLedgerMonth("2026-07")).toBe("2026-07")});it("genera CSV sin notas y neutraliza fórmulas",()=>{const malicious={...rows[0],restaurants:{id:"r1",name:"=CMD()",slug:"bad"}};const csv=manualPaymentsCsv([malicious]);expect(csv).toContain("'=CMD()");expect(csv).not.toContain("reference");expect(csv).not.toContain("notes")})});
